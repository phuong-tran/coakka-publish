# Historical Artifact Receipt: 1.3.1

This directory preserves the two metadata attachments from the former
`coakka-public-artifacts-v1.3.1` GitHub Release object before that object was
removed under the repository's warehouse-only release model.

The binary and package attachments already exist byte-for-byte in their
immutable versioned lanes. These two snapshots did not match the files stored
at the retained Git tag, so they are kept here to prevent metadata loss:

| File | SHA-256 |
| --- | --- |
| `public-artifacts.tsv` | `a86a74b0e2299c5ad040454edd7fffe3b2e0e871959d6e01d659e9865361669f` |
| `SHA256SUMS` | `b66578583fb19592f93631365e12663621521fa5a68d6ca77db87e5812093401` |

This is historical evidence, not the current artifact index. Use the root
`artifacts/public-artifacts.tsv` and lane-local manifests for current
coordinates.
