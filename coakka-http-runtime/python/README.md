# CoAkka HTTP Runtime for Python

Status: in development. Registry publication is not open.

The planned Python surface uses ordinary request, response, handler, stream,
server and client concepts. Capability completion, package assembly, platform
verification and runnable consumer samples are still in progress.

No PyPI package or installation command is declared by this development draft.
