# Runtime Artifact Note: `e2dc43a`

Date: 2026-05-17

This note summarizes the current aligned public runtime artifact set for native
package generation `0.1.0+e2dc43a`.

## Artifact Set

| Surface | Public artifact | Version |
| --- | --- | --- |
| Native C ABI | `runtime/native/releases/0.1.0+e2dc43a/coakka-runtime-native-v2-0.1.0.tar.gz` | `0.1.0+e2dc43a` |
| JVM runtime | `coakka.v2:coakka-jvm-native-runtime-v2` | `0.1.1-ge2dc43a-9227dc0` |
| Python runtime | `coakka_v2_connector` wheel | `0.1.0` |
| Node.js runtime | `coakka-v2-connector-node` package | `0.1.0` |
| Go runtime | `coakka-v2-connector-go` source package | `0.1.0` |
| C# runtime | `CoAkka.Runtime` NuGet package | `0.1.1` |
| Rust runtime | `coakka-runtime-rs` spike package | `0.1.0-spike` |
| Spring Boot adapter | `coakka.spring:coakka-spring-boot-starter` | `0.1.0-ge2dc43a` |
| Quarkus adapter | `coakka.quarkus:coakka-quarkus-extension` | `0.1.0-ge2dc43a` |

All language and framework artifacts in this set consume native package
generation `0.1.0+e2dc43a`.

## Runtime Changes

- Request-level failover is part of the runtime core path for clustered route
  candidates.
- Deadletter evidence now exposes attempt detail to native and JVM-side
  consumers.
- Transport failure stats now avoid stale failure state after successful
  recovery.
- The public native package keeps the runtime C ABI and scanner-clean TCP frame
  transport candidate for the supported public platforms.

## Verification

The artifact set was verified through the public publish gate:

- release `SHA256SUMS` files and root artifact manifest
- Maven checksum sidecars
- runtime JVM release and Maven jar native-entry parity
- runtime connector intake checks for JVM, Python, Node.js, Go, C#, and Rust

Consumer smoke checks were run for the packaged JVM, Python, Node.js, Go, C#,
and Rust lanes before staging this public set.

## Compatibility Rule

Use this set as a unit. Do not mix runtime language or framework packages from
another native package generation unless a later release note explicitly
declares that combination compatible.
