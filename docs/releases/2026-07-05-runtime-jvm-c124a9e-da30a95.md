# 2026-07-05 Runtime JVM c124a9e Refresh da30a95

This note records the JVM runtime connector refresh over native package
generation `0.2.0+c124a9e`.

| Surface | Public artifact | Version | Native package |
| --- | --- | --- | --- |
| JVM runtime | `coakka.v2:coakka-jvm-native-runtime-v2` | `0.2.0-gc124a9e-da30a95` | `0.2.0+c124a9e` |

The JVM connector code shape now follows source connector commit `da30a95`.
This drop keeps the same bundled native package generation and the same
embedded-native platform set as the previous `c124a9e` rebundle, while
refreshing the connector lifecycle/monitor line and public Maven/runtime
release metadata to the current connector state.

Current embedded-native coverage in the published jar remains:

- `macos-aarch64`
- `linux-aarch64`
- `linux-x86_64`

Windows host validation now exists downstream through explicit guest smoke and
runtime DLL override paths, but Windows native binaries are not bundled into
this public JVM jar yet.

Validation covered:

- JVM packaged-jar content verification
- JVM embedded-native smoke on macOS `aarch64`
- Windows 11 ARM guest downstream ask/deadletter plus lifecycle/monitor smoke
  against the same connector line with explicit runtime DLL injection
- manifest and native-entry hash parity against
  `runtime/native/releases/0.2.0+c124a9e/coakka-runtime-native-v2-0.2.0.tar.gz`
- Maven checksum sidecar verification
- public artifact manifest and surface verification

Spring Boot and Quarkus adapter dependency refresh over this runtime line is
recorded separately in
`2026-07-05-runtime-jvm-adapters-c124a9e-da30a95.md`.
