# Logger Bun 1.2.1 Refresh

This publishes the Bun logger connector package over native logger package
generation `1.2.1+f50756ebff0d` from connector source commit `6fdcc69`.

Published artifact:

| Artifact | Version | SHA256 |
| --- | --- | --- |
| `logger/bun/releases/1.2.1+f50756ebff0d-6fdcc69/coakka-logger-bun-1.2.1.tgz` | `1.2.1` | `481a963f2a64e1a0275e375222e28e5875bcb1a4c589c912bfa1f006d57ac98a` |

The package embeds the logger native libraries for macOS, Linux, and Windows on
`aarch64` and `x86_64`. Bun consumers install the package directly; the package
does not declare runtime package dependencies or require third-party native
runtime installation for the normal path.

Validation:

- Bun package build
- Bun connector tests
- packaged consumer smoke
- package dependency scan
- public surface scan

GitHub Release upload target:
`coakka-public-artifacts-v1.3.1`.
