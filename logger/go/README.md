# Logger Go Releases

This lane stores binary-only Go logger package drops.

Release directories live under:

```text
logger/go/releases/<native-package-version>/
```

Each release should include:

- `coakka-logger-go-<version>.tar.gz`
- `CONSUMING.md`
- `manifest.json`
- `SHA256SUMS`
