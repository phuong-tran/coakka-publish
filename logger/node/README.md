# Logger Node Releases

This lane stores binary-only Node.js logger package drops.

Release directories live under:

```text
logger/node/releases/<native-package-version>/
```

Each release should include:

- `coakka-logger-node-<version>.tgz`
- `CONSUMING.md`
- `manifest.json`
- `SHA256SUMS`
