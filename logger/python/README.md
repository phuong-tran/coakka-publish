# Logger Python Releases

This lane stores binary-only Python logger wheel drops.

Release directories live under:

```text
logger/python/releases/<native-package-version>/
```

Each release should include:

- `coakka_logger-<version>-*.whl`
- `CONSUMING.md`
- `manifest.json`
- `SHA256SUMS`
