# logger/zig

This lane stores source-first Zig logger connector drops.

Published releases live under:

```text
logger/zig/releases/<release-dir>/
```

Each release directory contains:

- `coakka-logger-zig-<version>-source.tar.gz`
- `manifest.json`
- `SHA256SUMS`
- `README.md`
- `CONSUMING.md`
